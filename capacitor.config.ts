import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MindsSDKDemo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
